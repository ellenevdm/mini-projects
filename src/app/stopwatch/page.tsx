import InProgress from "@/components/main/InProgress";
import { FC } from "react";

interface QuoteGeneratorPageProps {}

const StopWatchPage: FC = () => {
  return <InProgress appName="Stop Watch" />;
};

export default StopWatchPage;
